import 'package:flutter/material.dart';

class AppColors {
  // Primary - Deep Forest Green
  static const primary = Color(0xFF1B4332);
  static const primaryDark = Color(0xFF0D2B1F);
  static const primaryLight = Color(0xFF2D6A4F);

  // Accent - Orange
  static const accent = Color(0xFFE85D04);
  static const accentLight = Color(0xFFF48C06);

  // Backgrounds
  static const background = Color(0xFFF5F7FA);
  static const surface = Colors.white;
  static const cardGreen = Color(0xFFE8F5E9);

  // Status Colors
  static const success = Color(0xFF2E7D32);
  static const warning = Color(0xFFF9A825);
  static const error = Color(0xFFD32F2F);
  static const info = Color(0xFF1976D2);

  // Text
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B7280);
  static const textLight = Color(0xFF9CA3AF);
  static const textOnDark = Colors.white;

  // Borders & Dividers
  static const divider = Color(0xFFE5E7EB);
  static const border = Color(0xFFD1D5DB);
}